@model LearningApp.Models.Enrolment

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



    <section class="container">
        <div class="row ">
            <div class="col-lg-12 col-md-6 col-sm-12 bg-white">
                <div class="col-4">@Model.Student.Name</div>
                <div class="col-4">@Model.Course.Name</div>
                <div class="col-4">@Model.EnrolledAt.Value.ToString("dd MMMM yyyy")</div>
            </div>
            <div class="row">
                <div class="btn-group-sm" role="group">
                    <p class="m-1"> @Html.ActionLink("Edit", "Edit", "Enrolments", new { sid = Model.StudentID, cid = Model.CourseID}, new { @class = "btn btn-secondary" }) </p>
                    <p class="m-1"> @Html.ActionLink("Delete", "Delete", "Enrolments", new { sid = Model.StudentID, cid = Model.CourseID }, new { @class = "btn btn-secondary" }) </p>
                </div>
            </div>


        </div>
    </section>




@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

