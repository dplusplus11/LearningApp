@model LearningApp.Models.Category

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<h4  class="mb-4">Are you sure you want to delete this category?</h4>
<div>
   
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>

    @using (Html.BeginForm("DeletionConfirmed", "Categories"))
    {
        @Html.AntiForgeryToken()

    <div>
        @Html.ActionLink("Delete", "DeletionConfirmed", "Categories", new { id = Model.Id }, new { @class="btn btn-secondary mt-4"} )
    </div>
    }
</div>
